"""
.env.example — Copy this to .env and fill in your values.
(Only needed if PORTAL_URL changes from the hardcoded default)
"""

# ─── Portal URL (only override if the URL changes) ──────
# PORTAL_URL=https://aiprojects.odisha.gov.in/demo

# ─── Optional: custom upload sub-page URL ───────────────
# UPLOAD_PAGE_URL=https://aiprojects.odisha.gov.in/demo/verify

# ─── Custom Folders ─────────────────────────────────────
# OUTPUT_DIR=output
# LOGS_DIR=logs
# TEST_DATA_DIR=Test Data
